package com.analysis.mapper;

import com.analysis.model.Xs055;
import org.apache.ibatis.annotations.Param;

import java.util.List;

public interface Xs055Mapper {
    int insert(Xs055 record);

    int insertSelective(Xs055 record);

    List<Xs055> selectAll();

    List<Xs055> selectByDate(@Param("startDate") String startDate, @Param("endDate") String endDate);
}